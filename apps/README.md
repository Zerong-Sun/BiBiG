# 示例应用

本目录说明如何将 `skills/` 中的技能接入真实产品。**应用代码不是本仓库维护重点**；技能库才是核心交付物。

## 应用列表

| 应用 | 路径 | 说明 |
|------|------|------|
| BiBiG 口述传记 | [bibig/](bibig/) | 展示 `biography-writer` 如何驱动 LLM prompt |

新应用可在 `apps/<name>/` 添加 README，代码可放在仓库根目录或独立子目录。
